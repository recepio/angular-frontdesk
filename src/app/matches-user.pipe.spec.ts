import { MatchesUserPipe } from './matches-user.pipe';

describe('MatchesUserPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchesUserPipe();
    expect(pipe).toBeTruthy();
  });
});
